#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    cin.tie(0);
    cout.tie(0);
    ios_base::sync_with_stdio(0);
    unordered_map<int, int> vals;
    int max;
    cin>>max;
    pair<int,int> horses[max];
    for(int i=0;i<max;i++){
        int b,m;
        cin>>b>>m;
        horses[i]=pair<int,int>(b,m);
    }
    for(int i=0;i<max;i++){
        for(int j=i+1;j<max;j++){
            
        }
    }
}