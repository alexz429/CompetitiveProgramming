#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int arr[1000];

int main(){
    cin.tie(0);cout.tie(0);ios_base::sync_with_stdio(false);
    int n,q;
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    for(int i=0;i<q;i++){
        pair<ll,ll> number;
        
    }
}